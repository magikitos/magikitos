Use case: precise-object-edit.
Asset type: scale correction of a production eight-frame pixel-art needs animation.
Input image 1 is the edit target. Make ONE correction only: uniformly shrink every complete figure, including its cap, hands and held leaf, to 95 percent of its current width and height around the boot baseline in that figure's own cell. Keep the full canvas size, 4-column by 2-row grid, empty margins and each figure's horizontal center and boot baseline exactly unchanged. Do not crop or resize the full canvas. Do not redesign, repaint clothing, alter pose, rotate, change skin tone, add or remove details or change the relative sizes of body parts. The intention is a five-percent smaller same character within every existing cell, retaining the shorter crouched poses. Preserve the exact identity, crisp pixel-art treatment and genuine transparent alpha background. No grid, background, text, watermark or new elements.
