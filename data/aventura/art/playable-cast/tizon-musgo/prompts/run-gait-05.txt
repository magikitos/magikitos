Use case: precise-object-edit. IMAGE1 is the EDIT TARGET8x4 running sheet. IMAGE2 shows the correct four-phase FRONT running sequence ONLY. Fix the forward boot alternation in IMAGE1. Column1 FRONT: use the opposite legs/arm phases demonstrated image2, ordered top-to-bottom as image2 left-to-right. Keep image1 small-cell body proportions and matte style; image2 is pose reference only. In column5 BACK, mirror ONLY the LEGS in the bottom two rows so the leading boot switches image side versus top two rows. Do not mirror the head/cap/back braces or hip accessories. Keep all other columns exactly unchanged. Hip copper ring stays on SAME anatomical side through ALL frames. Preserve same8columns4rows grid, faces/hat sizes, clothes, clear cellspacing and transparent background. Only correct the alternating boot side in front and back views and opposing arm movement; no new design, no glossy style.
