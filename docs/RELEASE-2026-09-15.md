# Flat art, residents and island release

Owner-authorized production publication. The new long-term vision remains a
design discussion; no parcels, social backend or new quest ideas are implemented.

Candidate: `fe26c8158651ab28d10c`. Previous production artifact:
`4dd7398237af6fd39953`.

## Changes

- Flat art contract dated 2026-09-15, mirrored exactly from the website.
  Direct sheet grid, 300px thumbnails, selected master preview, paginated cursor,
  localized hub link for printing. No collection selector or intermediate page.
- Previously local mobility, camera-follow, pickup, 100-resident/Brizno,
  open-door and island improvements. See ISLET-AND-RESIDENTS.md,
  MOBILITY-BRIZNO.md and NAVIGATION.md.
- Website mount fails closed without leaking filesystem warnings into the HTML.
  This is error handling, not a claimed repair of Docker's filesystem.

## Evidence before activation

- Full core suite and flat-art DTO checks pass.
- Flat gallery browser checks pass five viewports, including 24+1 pagination,
  no master download before selection, return to grid and Escape.
- Real production API returns the same 18 sheet IDs/image URLs in all six
  languages. Staged candidate tested against real API and artwork at desktop,
  tablet and mobile. All mutation requests blocked.
- Immutable installer verifies inventory and checksums before staging.
  macOS archive metadata was rejected on the first transfer; a clean archive
  without AppleDouble/xattrs passed. Active production pointer was unchanged.
- No accounts, votes, chat, game saves, balances or database schemas changed.

## Local environment incident

The macOS pointer file is readable and valid, but DDEV reports input/output
errors. Docker's own daemon API times out and its VM log contains I/O failures
for multiple containers. This is separate from the repository extraction.
The static Node preview continues working without Docker.

Restarting Docker affects other projects and requires the owner's approval.
No volume deletion, database import, Docker reset or broad cleanup is authorized
by this release. Local DDEV availability must not be claimed until rechecked.

## Activation

Record the final commit pair and public smoke after the standard website
deployment. The previous artifact is retained for rollback and open clients.
